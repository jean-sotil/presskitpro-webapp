import type { EditorBundle } from '@/lib/editor/bundle';

export function PhotoGalleryRender(_: { bundle: EditorBundle }) {
  // Gallery binding lands in task-12 (Media → Profile gallery linkage).
  // No data in the bundle yet, so this section renders nothing today.
  return null;
}
