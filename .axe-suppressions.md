# Axe Rule Suppressions

Every entry here is an exception to PRD §11 / §19 (zero critical or
serious axe violations). Each suppression must include a justification,
a date, and the engineer's handle. Re-evaluate during every release-tag
a11y pass (`docs/qa/a11y-passes/`).

Format:

```
## <route> — <rule-id>

**Why:** <one-paragraph justification — what was tried, why it failed,
what the user-facing tradeoff is>.
**When:** <YYYY-MM-DD> — <handle>
**Revisit:** <YYYY-MM-DD or release tag>
```

> **No suppressions yet.** Task-25 closed the chrome layer at zero.
