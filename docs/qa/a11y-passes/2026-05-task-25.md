# 2026-05 — task-25 accessibility baseline

**Driver:** Automated (axe-core) + structural review. Manual NVDA / VoiceOver
pass scheduled for the v0.1.0 release tag (covered by `2026-XX-release-v0.1.md`).
**Routes covered:** /, /pricing, /login, /signup, /onboarding/{1..5},
/checkout/{pro-monthly,success,canceled}, /dashboard, /dashboard/analytics,
/dashboard/profile/<id>, /<seeded-slug>, paused-profile route.

## Summary
- Baseline established. Skip-to-content link mounted in the root layout;
  `lang="pt-BR"` set; every top-level `<main>` exposes `id="main"`.
- Axe e2e coverage extended to every route group above. Zero
  critical/serious violations on the chrome layer at the time of this pass.

## Findings
_None blocking. Future passes will list issues here as they surface._

## Notes
- The Stripe Checkout redirect is off-domain — we cannot run axe against
  it. Documented in `docs/runbooks/dev-editor.md`.
- Drag-reorder in the photo gallery editor uses keyboard fallbacks
  (arrow keys after grabbing) — verified during the pass; no
  drag-only interactions remain.
- Reduced-motion gates audited; existing call sites (`Grain`,
  `RevealStagger`, `LiveExamplesCarousel`, `AnchorNav`, `globals.css`)
  all honor `prefers-reduced-motion`.
